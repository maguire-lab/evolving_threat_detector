process {
    withName: PARSE_GBK {
        publishDir = [
            path: { "${params.outdir}/parsed/${meta.id}" },
            mode: 'copy',
            saveAs: { filename -> filename }
        ]
    }
}
